{
  "name": "fwe-lernplattform",
  "description": "This is the package.json file in root allow to run both frontend and backend with one cammand from the root",
  "scripts": {
    "start": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "start:backend": "cd backend && npm install && npm run build && npm start",
    "start:frontend": "cd frontend && npm install && npm run dev",
    "lint": "eslint --ext .ts src/",
    "test:unit": "jest --testPathPattern='tests/unit' --coverage --coverageDirectory=coverage",
    "test:integration": "jest --testPathPattern='tests/integration' --coverage --coverageDirectory=coverage-integration --reporter=jest-junit --outputFile=junit-integration.xml"
  },
  "devDependencies": {
    "@types/cors": "^2.8.18",
    "@types/express": "^5.0.2",
    "concurrently": "^9.1.2"
  }
}
